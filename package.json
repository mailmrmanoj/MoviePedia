{
  "version": "0.0.0",
  "private": true,
  "name": "moviePedia",
  "description": "A sample application for movie buffs",
   "devDependencies": {
    "bower": "^1.3.1"
   },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000",
    "pretest": "npm install",
    "preupdate-webdriver": "npm install"
   }
}
